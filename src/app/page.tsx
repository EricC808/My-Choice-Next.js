import HeroSection from '@/components/HeroSection';
import TopMenu from '@/components/TopMenu';
import './style.css';

/** The Home page. */
const Home = () => (
  <main>
    <TopMenu />
    <HeroSection />
  </main>
);

export default Home;
